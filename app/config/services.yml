# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    new_product_notify: ['fake@example.com']
    source_mail: 'fake@example.com'

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']
            
    AppBundle\Controller\ProductController:
        public: true
        arguments:
            $commandBus: '@simple_bus.command_bus'
            
    AppBundle\Listener\ProductAddListener:
        public: true
        arguments:
            $commandBus: '@simple_bus.command_bus'
        tags:
            - { name: doctrine.event_listener, event: postPersist }
            
    AppBundle\CommandHandler\AddProductHandler:
        public: true
        tags:
            - { name: command_handler, handles: AppBundle\Command\AddProductCommand }
            
    AppBundle\CommandHandler\MailProductAddedHandler:
        public: true
        arguments:
            $receivers: '%new_product_notify%'
            $from: '%source_mail%'
        tags:
            - { name: command_handler, handles: AppBundle\Command\MailProductAddedCommand }
            